{
  "name": "semantic-runtime-toolkit-go",
  "version": "0.1.0",
  "description": "Distributed agent coordination platform using Xiaomi MiMo for intelligent task decomposition and result aggregation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "start": "ts-node src/index.ts"
  },
  "keywords": ['ai-agents', 'llm-orchestration', 'mimo', 'workflow-automation'],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2",
    "vitest": "^1.6.0",
    "eslint": "^9.5.0"
  }
}